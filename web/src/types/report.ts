export interface BurnPoint{day:string;ideal_remaining:number;actual_remaining:number;cumulative_actual:number};export interface ProjectReport{project_id:string;budget_minutes:number;actual_minutes:number;remaining_minutes:number;burn_ratio:number;over_budget:boolean;forecast_minutes:number;points:BurnPoint[];cost_cents?:number};export interface CalendarDay{date:string;expected_minutes:number;reported_minutes:number;overtime_minutes:number;missing:boolean};export interface CalendarMonth{employee_id:string;timezone:string;days:CalendarDay[];total_minutes:number;overtime_minutes:number;missing_days:number}
