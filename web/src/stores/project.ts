import { defineStore } from 'pinia';import { api } from '../services/api';import type { ProjectReport } from '../types/report';export const useProjectStore=defineStore('project',{state:()=>({projectId:'project-demo',report:null as ProjectReport|null}),actions:{async load(id:string){this.projectId=id;this.report=await api<ProjectReport>(`/projects/${id}/report`)},async refresh(){await this.load(this.projectId)}}})
